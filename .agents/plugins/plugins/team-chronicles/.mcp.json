{
  "mcpServers": {
    "chronicle": {
      "command": "node",
      "args": ["./mcp/server.js"],
      "env": {
        "CHRONICLES_ROOT": "${CHRONICLES_ROOT}",
        "CHRONICLE_TEAM": "${CHRONICLE_TEAM}",
        "CHRONICLE_QUEUE": "${CHRONICLE_QUEUE}"
      }
    }
  }
}
