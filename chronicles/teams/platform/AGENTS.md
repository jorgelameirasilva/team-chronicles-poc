# Platform team chronicle rules

Extra constraints for `teams/platform/` dir.

- Service dirs under `services/<name>/` must have `overview.md`
- Runbooks land in `runbooks/`, one file per scenario
- Reference Grafana dashboards by full URL in `reference` type chronicles
- Oncall pager context → `runbooks/oncall.md` only
